  name: deploy Crafters Front
  on:  push
  jobs:
    lint:
      runs-on: ubuntu-latest
      steps:
        - name: get the repository code
          uses: actions/checkout@v3
        - name: Install dependencies
          run: npm ci
        - name: Lint Application
          run: npm run lint
    build:
      runs-on: ubuntu-latest
      steps:
        - name: get the repository code
          uses: actions/checkout@v3
        - name: Install dependencies
          run: npm ci
        - name: Build Application
          run: npm run build
        - name: Deploy
          run: echo Deploying...